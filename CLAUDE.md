# TapMap

Two independent projects in this repo: tapmap-api (port 4200) and tapmap-web (port 3000).
Separate package.json files, no shared node_modules, no monorepo tooling.
Use pnpm as package manager in both projects.
